generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id         String @id @default(uuid())
  name       String
  email      String @unique
  password   String
  profilePic String
  games      Game[]
}

model Game {
  id        String     @id @default(uuid())
  userId    String
  type      String
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  user      User       @relation(fields: [userId], references: [id])
  questions Question[]
}

model Question {
  id         String     @id @default(uuid())
  gameId     String
  dictId     String
  answerId   String
  question   String
  dictionary Dictionary @relation(fields: [dictId], references: [id])
  answer     Answer     @relation(fields: [answerId], references: [id])
  game       Game       @relation(fields: [gameId], references: [id])
}

model Answer {
  id         String     @id @default(uuid())
  answer     String
  questionId String
  gameId     String?
  Question   Question[]
}

model Dictionary {
  id         String     @id @default(uuid())
  word       String     @unique
  definition String
  question   Question[]
}
